CSP

考试:

t1:
签到题，花了半小时左右调完，刚开始做题状态还是比较好；

预计：100

t2:
通过栈模拟，相同的两数相消，不同直接加入，加入的元素是字母；
发现若两点模拟过程中栈相同，则两点构成的区间合法，用 hash 计数可以实现，拍了；

没有花多长去想和做。

预计：100

t3:
大模拟，用的递归写法，花了一个多小时，还调了半天，样例过完了，正解就是暴力，拍不了；
看到时间不多，直接去开 t4 了

预计：100

t4:
开题的时候还剩半个多小时，好在一眼秒了；
可以二分答案加贪心；

写出来发现被卡常了，还剩 5 分钟左右，结果卡常的时候把代码改错了，还没时间改回来。

预计：0

总：100 + 100 + 100 + 0

t1 -> t2 -> t3 -> t4

赛后：

实际得分： 100 + 50 + 15 + 0

t2:
发现交的是对拍时候的暴力代码，就只剩 50 分

t3:
大模拟，不知道那里挂了


反思：

时间利用不当，最后的时间没有去检查代码，反而用在了无意义卡常上面，导致了 t2 交错代码， t4 零分的结果；
t3 大模拟题容易挂，还花了大量时间去写，导致做 t4 的时间过短，并且实际也挂了只剩 15 分，做题的顺序也存在问题。

总体来说，虽然这套题并不难，但由于对于 csp 题目的太过自信，导致了错误的做题策略；
并且此次考试，我并没有先通看一遍题目，这也导致了我对考试策略的错估。
这使得 t3 花了大量时间只拿了少数的分，且开 t4 的时间过短，这一个错误使得为 t3 和 t4 基本相当于没做，也使得我没有发现 t2 的失误；

对于以后，我一定会贯彻正确的考试策略，尽量避免这次考试中出现的错误，使得自身的成绩更加稳定。